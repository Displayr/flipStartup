context("Subscription dot plot")
data(q.invoice.lines)
test_that(paste("Subscription dot plot"),
          {
#             expect_error(capture.output(), NA)
#            rd <- RevenueData(d$AUD, d$ValidFrom, d$ValidTo, end = end, id = d$name, by = "day", subset = d$validInvoice == 1)
#
# rd1 <-
#
#                             d1 <- d[d$ValidTo < ISOdate(2010,1,1), ]
#             ConnectedDotPlot(as.character(d1$name), d1$ValidFrom, d1$ValidTo)
 })



